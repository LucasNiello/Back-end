[ 👤 USUÁRIO / NAVEGADOR ]
                 |
        1. Requisição HTTP
   (ex: index.php?controller=livro)
                 |
                 v
      +-----------------------+
      |    🚦 index.php       |  <-- (ROTEADOR)
      |   (Front Controller)  |      Recebe tudo e decide quem chamar
      +----------+------------+
                 |
        2. Escolhe o Controller
                 |
      +----------v-----------+           +-----------------------+
      | 🎮 CONTROLLERS       |           | 🖥️ VIEWS (Interface)  |
      |                      |           |                       |
      | [ LivroController ]--+---------->| [ livros.php ]        |
      | [ EmprestimoController ]-+       | [ emprestimos.php ]   |
      +----------+-----------+   |       +-----------+-----------+
                 |               |                   |
        3. Chama Métodos (CRUD)  |        8. Retorna HTML Final
                 |               +-------------------+
                 v                                   |
      +----------------------+                       |
      | 🧱 MODELS / DAOs     |                       |
      |                      | <---------------------+
      | [ LivroDAO ]         |
      | [ EmprestimoDAO ]    |
      +----------+-----------+
                 |
        4. Pede Conexão Única
                 |
                 v
      +----------------------+
      | 🔌 Conexao (Singleton)| <-- Garante 1 conexão só
      +----------+-----------+
                 |
        5. Executa SQL
                 |
                 v
      +----------------------+
      | 🛢️ BANCO DE DADOS    |
      | (MySQL / Tabelas)    |
      +----------------------+

      Destaques Técnicos (O "Pulo do Gato")
Abra o código rapidamente nessas partes enquanto fala:

Arquitetura MVC: "Como podem ver na estrutura de pastas, separei a lógica de negócios (Model), a interface visual (View) e o controle de fluxo (Controller). O arquivo index.php atua como um Roteador, recebendo todas as requisições."

Padrão Singleton (Conexão): (Mostre o arquivo Conexao.php) "Para o banco de dados, implementei o Design Pattern Singleton. Isso garante que o sistema abra apenas uma conexão com o MySQL por requisição, economizando memória do servidor."

Segurança (PDO & Prepared Statements): (Mostre qualquer DAO, ex: LivroDAO.php) "Na segurança, fugi do básico. Usei PDO com Prepared Statements (bindValue). Isso blinda a aplicação contra ataques de SQL Injection, pois separa o comando SQL dos dados digitados pelo usuário."

Integridade de Dados (Transações): (Mostre o EmprestimoDAO.php) "Esta é a parte crítica. Um empréstimo envolve duas ações: criar o registro e baixar o estoque. Usei Transações (beginTransaction, commit, rollback). Se o estoque falhar, o empréstimo não é salvo. Isso garante que o banco nunca fique inconsistente."

3. Design e UX (A Experiência do Usuário)
(Abra o navegador)

Identidade Visual: "Para o front-end, criei um tema inspirado na estética 'utilitária/industrial' (estilo The Last of Us), focando em legibilidade e contraste, sem usar frameworks pesados como Bootstrap. É CSS puro."

Usabilidade (Busca Dinâmica): "No formulário de empréstimo, implementei um campo de busca inteligente com Javascript e Datalist. O usuário digita o nome do livro, mas o sistema envia o ID oculto para o banco, facilitando a vida do bibliotecário."


O que acontece se eu tentar excluir um livro que está emprestado? R: "O banco de dados vai bloquear. Configurei uma Foreign Key com ON DELETE RESTRICT. Além disso, meu Controller captura esse erro específico (código 23000) e exibe uma mensagem amigável para o usuário dizendo que ele precisa devolver o livro antes de excluir."

Onde está a lógica de diminuir o estoque? R: "Está encapsulada no LivroDAO, mas é chamada dentro da transação do EmprestimoDAO. Assim mantenho o código organizado."