Diferente dos arrays convencionais que associam os valores armazenados no array a um índice numérico (0, 1, 2…), os arrays associativos permitem que você use chaves personalizadas para cada valor.

Em vez de contar a posição, você dá um nome para cada valor, e depois acessa o valor usando esse nome.

Exemplo:

$aluno = [
    "id" => 1,
    "nome" => "Josias",
    "curso" => "Engenharia"
];


Aqui:

"id", "nome" e "curso" são as chaves.

1, "Josias" e "Engenharia" são os valores.

Para pegar o nome do aluno, você não precisa contar posições, só faz:

echo $aluno["nome"]; // Saída: Josias


Resumo:

Array normal → acessa pelo número da posição ($array[0]).

Array associativo → acessa pelo nome da chave ($array["nome"]).

======

Beleza, vamos ver como ficaria o mesmo conjunto de alunos em JSON. 😎

No formato JSON:

{
  "1": {
    "nome": "Josias Silva",
    "curso": "Engenharia"
  },
  "2": {
    "nome": "Josias Oliveira",
    "curso": "Manicure"
  },
  "3": {
    "nome": "Beatriz",
    "curso": "Eletricista"
  },
  "4": {
    "nome": "Aurora",
    "curso": "Arquitetura"
  },
  "5": {
    "nome": "Oliver",
    "curso": "Gestão"
  }
}


Explicando de forma simples:

As chaves externas (1, 2, 3…) são os IDs únicos, igual no array associativo.

Cada valor é outro objeto JSON que contém os dados do aluno (nome, curso, etc.).

Se quiser pegar o aluno com ID 2, você acessa "2" e pega nome e curso.